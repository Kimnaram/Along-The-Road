<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.along_the_road">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TrafficSearchActivity"></activity>
        <activity android:name=".HotelSelectActivity" />
        <activity android:name=".localselectActivity" />
        <activity android:name=".DaySelectActivity" />
        <activity android:name=".managebudgetActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="ujnor2ft1p" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCClHCv72sCj8-8jyzmVimeRiaBfwsOi4E" />

    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>